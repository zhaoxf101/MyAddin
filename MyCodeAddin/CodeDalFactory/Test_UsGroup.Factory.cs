using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using EF;
using EF.Data;
using EF.Linq;
using EF.Server;
using EFramework.DalLinq;

namespace Common.DalFactory
{
	public class Test_UsGroupFactory:Common.Business.Test_UsGroup
    {
        public static Common.Business.Test_UsGroup Fetch(Test_UsGroup data)
        {
            Common.Business.Test_UsGroup item = (Common.Business.Test_UsGroup)Activator.CreateInstance(typeof(Common.Business.Test_UsGroup));
            //using (ObjectFactory.BypassPropertyChecks(item))
            {
				                item.UserGroup = data.UserGroup;
				                item.DText = data.DText;
				                item.Cancelled = data.Cancelled;
				                item.Approved = data.Approved;
				                item.CreatedUser = data.CreatedUser;
				                item.CreatedDate = data.CreatedDate;
				                item.ChangedUser = data.ChangedUser;
				                item.ChangedDate = data.ChangedDate;
							}
			ObjectFactory.MarkAsChild(item);
            ObjectFactory.MarkOld(item);
            return item;
        }

		private void DataPortal_Fetch(Common.Business.LambdaExpression lambda)
        {
            using (var ctx = ContextManager<EFDataContext>.GetManager(Database.EFramework))
            {
				var exp = lambda.Resolve<Test_UsGroup>();
                var i = (from p in ctx.DataContext.Test_UsGroup.Where(exp)
                         select p).FirstOrDefault();
                if (i != null)
                {
										this.UserGroup = i.UserGroup;
										this.DText = i.DText;
										this.Cancelled = i.Cancelled;
										this.Approved = i.Approved;
										this.CreatedUser = i.CreatedUser;
										this.CreatedDate = i.CreatedDate;
										this.ChangedUser = i.ChangedUser;
										this.ChangedDate = i.ChangedDate;
					}
            }
        }
	}

	public class Test_UsGroupsFactory : Common.Business.Test_UsGroups
    {
        private void DataPortal_Fetch()
        {
            using (var ctx = ContextManager<EFDataContext>.GetManager(Database.EFramework))
            {
                var i = from p in ctx.DataContext.Test_UsGroup
                        select Test_UsGroupFactory.Fetch(p);
                this.RaiseListChangedEvents = false;
                this.IsReadOnly = false;
                this.AddRange(i);
                this.IsReadOnly = true;
                this.RaiseListChangedEvents = true;
            }
        }

		private void DataPortal_Fetch(Paging page)
        {
            using (var ctx = ContextManager<EFDataContext>.GetManager(Database.EFramework))
            {
                var i = (from p in ctx.DataContext.Test_UsGroup
                        select Test_UsGroupFactory.Fetch(p)).Skip(page.StartIndex).Take(page.RowCount);
                this.RaiseListChangedEvents = false;
                this.IsReadOnly = false;
                this.AddRange(i);
                this.IsReadOnly = true;
                this.RaiseListChangedEvents = true;
            }
        }

        private void DataPortal_Fetch(PagigExpress page)
        {
            using (var ctx = ContextManager<EFDataContext>.GetManager(Database.EFramework))
            {
                var exp = page.Lambda.Resolve<Test_UsGroup>();
                var i = (from p in ctx.DataContext.Test_UsGroup.Where(exp)
                         select Test_UsGroupFactory.Fetch(p)).Skip(page.StartIndex).Take(page.RowCount);
                this.RaiseListChangedEvents = false;
                this.IsReadOnly = false;
                this.AddRange(i);
                this.IsReadOnly = true;
                this.RaiseListChangedEvents = true;
            }
        }

		private void DataPortal_Fetch(Common.Business.LambdaExpression lambda)
        {
            using (var ctx = ContextManager<EFDataContext>.GetManager(Database.EFramework))
            {
                var exp = lambda.Resolve<Test_UsGroup>();
                var i = from p in ctx.DataContext.Test_UsGroup.Where(exp)
                         select Test_UsGroupFactory.Fetch(p);
                this.RaiseListChangedEvents = false;
                this.IsReadOnly = false;
                this.AddRange(i);
                this.IsReadOnly = true;
                this.RaiseListChangedEvents = true;
            }
        }


    }
}
